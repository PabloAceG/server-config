---
- hosts: pve

  tasks:
    - name: Create Docker common network for dashboard
      docker_network:
        name: dashboard

    - include: grafana/deploy-grafana.yml
      tags: grafana

    - include: prometheus/deploy-prometheus.yml
      tags: prometheus

    - include: node_exporter/deploy-node-exporter.yml
      tags: nexporter

    - include: nginx/deploy-nginx.yml
      tags: nginx
