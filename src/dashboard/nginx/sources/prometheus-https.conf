server {
    listen 9091 ssl;
    listen [::]:9091 ssl;

    server_name localhost;

    location /prometheus {
      proxy_pass		http://docker-prometheus;
      proxy_buffering off;

      proxy_set_header Host $host;
    }
}
